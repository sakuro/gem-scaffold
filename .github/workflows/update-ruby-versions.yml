name: Update Ruby Versions

# Runs twice a year to keep .ruby_versions.json up-to-date with all maintained Ruby versions
# Data source: Ruby's official branches.yml from www.ruby-lang.org repository
on:
  schedule:
    # Run at 13:23 UTC on Jan 2-8 and Apr 2-8 (1-week window aligned with Ruby's release schedule)
    - cron: '23 13 2-8 1,4 *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v5

      - name: Generate ruby.json
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          gh api -H "Accept: application/vnd.github.raw" repos/ruby/www.ruby-lang.org/contents/_data/branches.yml | \
            yq -o json | \
            jq '{ruby: [.[] | select(.status | test("maintenance")) | {name, date}] | sort_by(.date) | map(.name | tostring)}' \
            > .ruby_versions.json

      - name: Update Ruby version in configuration files
        run: |
          min_version=$(jq -r '.ruby[0]' .ruby_versions.json)
          sed -i "s/TargetRubyVersion: [0-9.]\+/TargetRubyVersion: $min_version/" .rubocop.yml
          sed -i "s/ruby = \"[0-9.]\+\"/ruby = \"$min_version\"/" mise.toml
          sed -i "s/ruby-version: \"[0-9.]\+\"/ruby-version: \"$min_version\"/" .github/workflows/release-validation.yml
          sed -i "s/ruby-version: \"[0-9.]\+\"/ruby-version: \"$min_version\"/" .github/workflows/release-publish.yml

          # Find gemspec file (name varies per gem)
          gemspec_file=$(ls *.gemspec | head -n 1)
          if [ -n "$gemspec_file" ]; then
            sed -i "s/required_ruby_version = \">= [0-9.]\+\"/required_ruby_version = \">= $min_version\"/" "$gemspec_file"
          fi

      - name: Setup Git User
        uses: git-actions/set-user@v1

      - name: Create Pull Request
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          # Check if there are changes
          if git diff --quiet .ruby_versions.json .rubocop.yml mise.toml .github/workflows/release-*.yml *.gemspec; then
            echo "No changes detected"
            exit 0
          fi

          # Create branch and commit
          branch="update-ruby-versions"
          git checkout -b "$branch"
          git add .ruby_versions.json .rubocop.yml mise.toml .github/workflows/release-*.yml *.gemspec
          git commit -m ":arrow_up: Update Ruby versions and version requirements"
          git push -f origin "$branch"

          # Check if PR already exists
          if gh pr list --head "$branch" --json number -q '.[0].number' | grep -q .; then
            echo "PR already exists"
            exit 0
          fi

          # Create PR body in temporary file
          cat > /tmp/pr-body.md <<'EOF'
          Automated update of maintained Ruby versions from Ruby's official branches.yml.

          This PR updates:
          - `.ruby_versions.json` with all maintained Ruby versions
          - `.rubocop.yml` TargetRubyVersion to match the minimum Ruby version
          - `mise.toml` ruby version to match the minimum Ruby version
          - `.github/workflows/release-*.yml` ruby-version to match the minimum Ruby version
          - `*.gemspec` required_ruby_version to match the minimum Ruby version
          EOF
          gh pr create --title ":arrow_up: Update Ruby versions" --body-file /tmp/pr-body.md
